<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.1</TargetFramework>
        <LangVersion>8.0</LangVersion>
        <NullableContext>Enabled</NullableContext>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <PackageVersion>1.0.0</PackageVersion>
        <Title>MockServer</Title>
        <Authors>Alexander Held</Authors>
        <Description>A C# Client for the MockServer Docker Image</Description>
        <Copyright>Alexander Held © 2019</Copyright>
        <PackageProjectUrl>https://www.github.com/alex-held/HardCode.MockServer</PackageProjectUrl>
        <RepositoryUrl>https://www.github.com/alex-held/HardCode.MockServer</RepositoryUrl>
        <RepositoryType>github</RepositoryType>
        <PackageTags>mockserver</PackageTags>
        <Company>Alexander Held</Company>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
      <DebugSymbols>true</DebugSymbols>
      <DebugType>full</DebugType>
      <DocumentationFile>bin\Release\HardCoded.MockServer.xml</DocumentationFile>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Newtonsoft.Json" Version="12.0.2" />
    </ItemGroup>

</Project>
